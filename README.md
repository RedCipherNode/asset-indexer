# Workspace Indexer

A Python CLI tool for scanning workspace directories and preparing file inventories.

## Current Features

- Recursive directory scanning
- File counting
- File size collection

## Run Locally

```powershell
$env:PYTHONPATH = "src"
python -m workspace_indexer "."